services:

  frontend:
    build: ./frontend
    ports:
      - "9000:80"

  backend:
    build: ./backnend
    environment:
      - DATABASE_PATH=/app/data/tasks.db
    volumes:
      - task-data:/app/data
    ports:
      - "5000:5000"

volumes:
  task-data:  
